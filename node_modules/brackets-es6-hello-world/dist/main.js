define(function (require) {
  "use strict";

  var AppInit = brackets.getModule("utils/AppInit");
  var co = require("bluebird").coroutine;
  var Logger = require("./utils/Logger");
  var SUCCESS = Symbol();

  function promiseResponse() {
    return new Promise(function (resolve) {
      setTimeout(function () {
        resolve(SUCCESS);
      }, 1);
    });
  }

  var tryGenerators = co(function* () {
    var response = yield promiseResponse();
    if (response === SUCCESS) {
      Logger.log("Hello world!");
    } else {
      throw new Error("Response has unexpected value: " + response);
    }
  });

  AppInit.appReady(function () {
    // co functions return promises
    tryGenerators()["catch"](function (e) {
      return Logger.error(e);
    });
  });
});
//# sourceMappingURL=/Users/arajasekaran/WebstormProjects/ECMA6 Js Demo/node_modules/brackets-es6-hello-world/dist//main.js.map
